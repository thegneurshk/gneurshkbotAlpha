module.exports = {
    name: 'test',
    description: "this is a test command",
    execute(message, args){
        message.channel.send('test clear');
        //random facts
        //triscuit is short for electricity biscuit, they were the first biscuits to be made with electricity
        //a chinese emperor once escaped an assassin by running in circles around a pillar
        //two prime numbers with a difference of 6 are known as sexy primes
        //when a spider dies, its legs curl up due to a loss of blood pressure
        //
    }
}
